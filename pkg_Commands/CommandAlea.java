package pkg_Commands;
import pkg_Core.*;
import pkg_GameElements.*;

/**
 * Classe pour la commande alea , qui permet de forcer le résultat de la transporter Room
 * 
 * @author  Erwan Mailharro
 * @version 10/04/16
 */
public class CommandAlea extends Command
{

    /**
     * Constructeur pour les objets de la classe CommandAlea
     */
    public CommandAlea()
    {

    }

    /**
     * Permet d'executer la commande 
     * @param  player décrit le player qui execute l'action
     */
    public void execute(Entity player)
    {
// 
//         TransporterRoom vRoom = (TransporterRoom) player.getEngine().getRoomList()[7];
//         if (this.hasSecondWord()) vRoom.rig(player.getEngine().getRoomList()[Integer.parseInt(this.getSecondWord())]);
//         else vRoom.rig(null);
    }
}
